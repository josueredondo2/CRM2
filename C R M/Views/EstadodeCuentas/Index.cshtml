@model IEnumerable<C_R_M.Models.EstadodeCuenta>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id_Credito_Disponible)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Empresa1.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Id_Credito_Disponible)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Empresa1.Nombre)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.Id_Estado }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.Id_Estado }) |
            @Html.ActionLink("Borrar", "Delete", new { id=item.Id_Estado })
        </td>
    </tr>
}

</table>
