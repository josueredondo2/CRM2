@model IEnumerable<C_R_M.Models.EstadodeCuenta>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id_Credito_Disponible)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Empresa1.Nombre)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id_Credito_Disponible)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Empresa1.Nombre)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id_Estado }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id_Estado }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id_Estado })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
